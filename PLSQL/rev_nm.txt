declare
     n varchar2(10) := '5784';
     slen number(5) := 0;
     inum varchar2(10) := 0;
begin
        slen=length(n);
     for i in reverse 1..slen loop
          inum := inum || substr(n,1,1);
      end loop;
        dbms_output.put_line(inum);
end;
/

<label>[quiz quiz-403 "4+8? | 12" "6*3 ? | 18" "4-3 ? |1"]</label>